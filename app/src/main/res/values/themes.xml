<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme with Material3 styling and no ActionBar -->
    <style name="Base.Theme.AndroidLabs" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Main application theme without ActionBar -->
    <style name="Theme.AndroidLabs" parent="Base.Theme.AndroidLabs">
        <!-- Further customizations for the application theme -->
    </style>

    <!-- Alternative theme with ActionBar included -->
    <style name="Theme.AndroidLabs.ActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your ActionBar theme here -->
    </style>

    <!-- Items related to window styling should be placed inside the correct style -->
    <style name="CustomActionBarStyle">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>
